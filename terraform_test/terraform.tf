#This is useful to prevent compatibility issues with older versions. But will cause problems because it is unlikely dev are all on the latest and hard to upgrade the larger the software becomes.
terraform {
  required_version = ">= 1.15.6"
}
